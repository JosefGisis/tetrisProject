# programming tools

# this tool returns left or top of an object of a given width/height on a surface of a given width/height
def center(surface_size, object_size):
    cntr = (surface_size - object_size) // 2
    return cntr